(function (window, document) {
    var template = '{{template}}',
        style = '{{style}}';

    function init() {
        return {
            name: 'dialog',
            version: '1.0.0'
        };
    }
123213123213
    window.dialog = init();
})(window, document);