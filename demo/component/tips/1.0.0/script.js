(function (window, document) {
    var template = '{{template}}';

    function init() {
        return {
            name: 'tips',
            version: '1.0.0'
        };
    }

    window.tips = init();
})(window, document);